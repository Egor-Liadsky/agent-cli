## 1. Захват мыши и прокрутка колесом

- [x] 1.1 В `run` (`crates/cli/src/tui.rs:1537-1556`) добавить
  `EnableMouseCapture` к `execute!` при входе в альтернативный экран и
  `DisableMouseCapture` в последовательность восстановления терминала,
  заменив прежний комментарий про отказ от захвата на объяснение нового
  компромисса; проверка — `cargo run -p agentcli -- chat` запускается и
  после выхода терминал не шлёт в оболочку escape-последовательности мыши.
- [x] 1.2 Добавить в `AppState` поле `history_areas: Vec<(String, Rect)>`,
  очищать его в начале `render_ui` и дописывать область истории чата в
  `render_pane`; проверка — `cargo build` и визуально: при одной панели
  вектор содержит одну запись (временный `debug`-вывод или тест на
  вспомогательной функции попадания точки).
- [x] 1.3 Написать вспомогательную функцию поиска чата по координате
  курсора (`Rect` содержит `column`/`row`) и покрыть её юнит-тестом на
  попадание внутрь, на границу и мимо всех областей; проверка —
  `cargo test -p agentcli`.
- [x] 1.6 Убрать прокрутку истории стрелками `↑`/`↓` из `handle_input_key`,
  оставив `PageUp`/`PageDown`, и обновить подсказку и README; проверка —
  `cargo test -p agentcli`, стрелки в поле ввода историю не двигают.

- [x] 1.4 В цикле `run_app` обработать `Event::Mouse` с
  `MouseEventKind::ScrollUp`/`ScrollDown`: шаг 3 строки, прокрутка вверх
  сбрасывает `auto_scroll`, вниз — выставляет `auto_scroll = scroll >=
  max_scroll`, прочие `MouseEventKind` игнорируются; проверка — в запущенном
  TUI колесо листает историю и новые ответы перестают доскроливаться, пока
  история не доведена до низа.
- [x] 1.5 Игнорировать события колеса, когда открыто модальное окно
  (`state.focus` — `Settings`, `Import`, `Confirm`, `Facts`, `Branches`,
  `Memory`, `Task`) или курсор вне областей истории; проверка — при открытом
  `Ctrl+P` колесо над историей её не двигает.

## 2. Режим выбора сообщения

- [x] 2.1 Добавить вариант `Focus::MessageSelect` и поле
  `ChatUi.selected_message: Option<usize>`; проверка — `cargo build` без
  предупреждений о неполных `match` по `Focus`.
- [x] 2.2 В `handle_global_key` добавить вход в режим по `Ctrl+G`
  (и `Char('п')` для русской раскладки) из `Focus::Input`/`Focus::Sidebar`:
  выбирается последнее сообщение, а на пустой истории режим не открывается и
  показывается уведомление; проверка — обе ветки воспроизводятся в TUI.
- [x] 2.3 Написать обработчик клавиш режима: `↑`/`↓` двигают выбор с
  упором в границы, `Esc` выходит без завершения TUI, `Enter` копирует и
  выходит, `Ctrl+Y` копирует и оставляет режим открытым; проверка —
  `cargo test -p agentcli` для чистой функции смещения выбора плюс ручной
  прогон в TUI.
- [x] 2.4 Подсветить выбранное сообщение в `render_history` (стиль на строке
  заголовка `● Вы` / `● Агент`) и доводить его в видимую область через
  существующий механизм `scroll_to_message`/`target_line`; проверка — в TUI
  выбор длинного сообщения прокручивает историю к нему.
- [x] 2.5 Ограничивать `selected_message` длиной истории после её
  перезагрузки и сбрасывать в `None` для пустого чата; проверка — юнит-тест
  на функцию нормализации выбора.

## 3. Копирование текста сообщения

- [x] 3.1 Копировать `Message.content` выбранного сообщения через
  `crate::clipboard::copy`, не добавляя заголовок роли, метрики токенов и
  `Message.reasoning`; проверка — скопированный из TUI ответ вставляется в
  редактор как чистый исходный текст.
- [x] 3.2 Показывать уведомление об успехе и, при ошибке буфера обмена,
  уведомление с причиной, не прерывая работу TUI; проверка — ручной прогон
  успешного пути и запуск без доступа к дисплею (`WAYLAND_DISPLAY=
  DISPLAY= cargo run -p agentcli -- chat` на Linux либо ssh-сессия).

## 4. Подсказки и документация

- [x] 4.1 Дополнить строку подсказки (`render_help`) клавишами режима
  выбора, когда `state.focus == Focus::MessageSelect`; проверка — подсказка
  меняется при входе в режим и возвращается прежней после `Esc`.
- [x] 4.2 Обновить README `agent-cli`: прокрутка колесом в истории,
  `Shift`+перетаскивание для нативного выделения при включённом захвате
  мыши, `Ctrl+G` и клавиши копирования сообщения; проверка — раздел
  горячих клавиш перечисляет все новые сочетания.

## 5. Проверка целиком

- [x] 5.1 Прогнать `cargo test` и `cargo build --release` в `agent-cli`;
  проверка — обе команды завершаются успешно.
- [ ] 5.2 Пройти сценарии спеки вручную в двух терминалах (например,
  iTerm2 и Terminal.app): колесо над каждой из двух панелей, `Shift`-
  выделение, копирование сообщения, возврат автопрокрутки при доведении
  истории до низа; проверка — все сценарии
  `specs/chat-history-navigation/spec.md` воспроизводятся.
