## 1. Дозапись сообщений в сервисе (репозиторий `agent-sever`)

- [x] 1.1 Добавить в `src/store.rs` функцию дозаписи списка готовых сообщений в чат владельца одной транзакцией (номера `seq` назначает хранилище, время изменения чата обновляется) и проверить тестом модуля `store`, что при отказе на втором сообщении из двух в чате не остаётся ни одного
- [x] 1.2 Добавить в `src/dto.rs` тело запроса дозаписи (`messages` с ролью, текстом, необязательными рассуждением, моделью, `usage` и `timing`) и ответ с назначенными номерами; проверить, что тело с полем `api_key` не разбирается как валидное
- [x] 1.3 Добавить маршрут `POST /v1/chats/{id}/messages` в `src/app.rs` с владением по отпечатку клиентского токена, без обращения к провайдеру и без стадий конвейера; проверить тестом в `src/tests.rs`, что при полностью отключённом моке провайдера запрос завершается успехом
- [x] 1.4 Покрыть тестами в `src/tests.rs` отказы: пустой массив `messages` и неизвестная роль дают `400`, чужой и несуществующий чат — одинаковый `404`, ошибки приходят в едином конверте с `request_id` и заголовком `x-request-id`
- [x] 1.5 Проверить тестом, что `GET /v1/chats/{id}` возвращает дозаписанные сообщения в порядке запроса с сохранённой телеметрией, а чат поднимается в `GET /v1/chats` как недавно изменённый
- [x] 1.6 Проверить тестом, что при выключенном `AGENTD_LOG_CONTENT` в журнале есть запись о дозаписи с идентификатором чата и числом сообщений, но нет текста сообщений
- [x] 1.7 Описать новый эндпоинт в README `agent-sever` и прогнать `cargo test` в этом репозитории
- [x] 1.8 Разделить проверку настроек чата на «для хранения» и «для вызова модели» в `src/app.rs`: чат с провайдером `ollama` и локальной моделью создаётся и меняется без отказа, а белый список моделей и наличие `AGENTD_OLLAMA_URL` проверяются при `POST /v1/chat`; проверить тестами создание и `PATCH` локального чата при незаданном `AGENTD_OLLAMA_URL`
- [x] 1.9 Проверить тестами в `src/tests.rs`, что `POST /v1/chat` с `chat_id` локального чата даёт `400` при ненастроенном Ollama и не меняет историю, а облачный чат с моделью вне белого списка по-прежнему отклоняется при создании

- [x] 1.10 Разрешить в `src/dto.rs` полное задание настроек чата: явный `null` у параметров сэмплирования сбрасывает их, поле `custom_response_mode` включает и выключает кастомный формат, отсутствие поля оставляет значение прежним; проверить тестами в `src/tests.rs` сброс температуры, выключение кастомного формата и неизменность незаданных полей

## 2. Клиент чатов в крейте `agentclient`

- [x] 2.1 Добавить в `crates/client` тип `ChatsClient` с адресом сервиса, клиентским токеном и журналом обмена; проверить тестом на `wiremock`, что запрос с непустым токеном несёт `Authorization: Bearer`, а с пустым уходит без этого заголовка
- [x] 2.2 Реализовать `GET /v1/chats` с дочитыванием страниц по `next_cursor` и проверить тестом, что из двух страниц собирается один список в порядке сервиса
- [x] 2.3 Реализовать `GET /v1/chats/{id}` с дочитыванием сообщений по `next_after` и проверить тестом, что история из двух страниц собирается целиком и в порядке номеров
- [x] 2.4 Реализовать `POST /v1/chats`, `PATCH /v1/chats/{id}` и `DELETE /v1/chats/{id}`; проверить тестами тело каждого запроса и разбор ответа
- [x] 2.5 Реализовать `POST /v1/chats/{id}/messages` с обеими репликами в одном запросе и проверить тестом, что телеметрия ответа модели уходит в теле, а поля `api_key` в нём нет ни на каком уровне
- [x] 2.6 Отобразить конверт ошибок сервиса в типизированную `AgentError` тем же способом, что в `ServerAgent`, и проверить тестами, что `401`, `400`, `404`, `429`, `502` и недоступный адрес различимы через `downcast_ref`, а `request_id` попадает в сообщение
- [x] 2.7 Прогнать `cargo test -p agentclient`

## 3. Пустой и незагруженный список чатов в TUI

- [x] 3.1 Заменить `AppState.chats` на состояние с исходами «загружается», «загружен» и «отказ с причиной», сохранив `ChatSession` как модель в памяти; убедиться, что `cargo build` проходит
- [x] 3.2 Перевести `active_chat_id` и `chat_index` на `Option` вместо `expect`, а обращения к активному чату — на обработку отсутствия; проверить, что при пустом списке чатов и пустых `panes` отрисовка и обработка клавиш не приводят к панике
- [x] 3.3 Убрать вставку чернового чата при старте и показывать вместо него состояние загрузки; проверить, что запуск TUI без чатов открывается с пустым списком

## 4. Операции над чатами через сервис

- [x] 4.1 Загружать список чатов фоном при старте через `tokio::spawn` и `ChatEvent`; проверить, что список в TUI совпадает с ответом `GET /v1/chats`, а порядок — от недавно изменённого к старому
- [x] 4.2 Показывать баннер при отказе загрузки с причиной, адресом сервиса и `request_id`, добавить повтор загрузки; проверить, что после запуска поднятого сервиса повтор заполняет список без перезапуска клиента, а при `401` баннер содержит подсказку про `agentcli config set-token`
- [x] 4.3 Запретить создание чата и отправку сообщения, пока список чатов не загружен, показывая ту же причину; проверить обе попытки в TUI
- [x] 4.4 Загружать историю чата при первом открытии, блокируя ввод в панели до её получения; проверить, что сообщения, заголовок и параметры берутся из `GET /v1/chats/{id}`
- [x] 4.5 Перевести создание чата на `POST /v1/chats` с умолчаниями из конфига клиента и идентификатором из ответа сервиса; проверить, что чат виден после перезапуска
- [x] 4.6 Перевести переименование и изменение параметров чата на `PATCH /v1/chats/{id}` по подтверждению в редакторе настроек; проверить, что изменение сохраняется после перезапуска и что перебор моделей стрелками не порождает запросов
- [x] 4.7 Перевести удаление чата на `DELETE /v1/chats/{id}`; проверить, что чат исчезает из списка и не появляется после перезапуска
- [x] 4.8 Оставлять список в подтверждённом сервисом состоянии при отказе создания, переименования или удаления и показывать причину с `request_id`; проверить на замоканном отказе сервиса
- [x] 4.9 Строить перенос контекста по истории, полученной от сервиса, и запрещать его, пока история чата-источника не загружена; проверить, что время изменения чата-источника от переноса не меняется

## 5. Запись обмена

- [x] 5.1 Передавать `chat_id` в `POST /v1/chat` для чатов с провайдером `cloud` и убрать клиентскую дозапись для них; проверить тестом `ServerAgent`, что поле уходит в теле, и в TUI — что обе реплики видны после перезапуска
- [x] 5.2 Дозаписывать обмен чата с провайдером `ollama` одним запросом `POST /v1/chats/{id}/messages` после ответа локальной модели; проверить, что после перезапуска видны обе реплики с телеметрией
- [x] 5.3 Помечать чат несохранённым при отказе дозаписи, сохраняя ответ на экране, показывая причину с `request_id` и давая повторить дозапись; проверить на замоканном отказе сервиса
- [x] 5.4 Отправлять заголовок, выведенный из первой реплики, запросом `PATCH /v1/chats/{id}`, не перезаписывая заголовок, заданный пользователем вручную, и не меняя его при переносе контекста; проверить все три случая

## 6. Удаление локального хранилища

- [x] 6.1 Удалить из `crates/cli/src/chats.rs` файловые функции и `chats_dir`, оставив `ChatSession`, `context_block` и `last_activity_label`; проверить, что `cargo build` проходит и в крейте не осталось обращений к каталогу чатов
- [x] 6.4 Проверить, что после создания чата, обмена сообщениями и выхода из клиента клиент не пишет новых файлов с текстом сообщений или параметрами чата в каталогах конфигурации и данных

## 7. Приёмка

- [x] 7.1 Обновить README `agent-cli`: хранение чатов в сервисе, поведение без сервиса, отсутствие локальных файлов чатов
- [x] 7.2 Прогнать `cargo test` в `agent-cli` и `cargo test` в `agent-sever`
- [x] 7.3 Проверить вживую чат обоих провайдеров против запущенного `agentd`: обмен, перезапуск клиента, недоступность сервиса и повтор загрузки
