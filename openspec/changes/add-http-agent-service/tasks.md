## 1. Workspace и перенос ядра (репозиторий agent-cli)

- [x] 1.1 Превратить корневой `Cargo.toml` в манифест workspace с членами
  `crates/core` и `crates/cli`; проверка: `cargo metadata --no-deps` показывает
  два пакета, `agentcore` и `agentcli`.
- [x] 1.2 Создать `crates/core` (`agentcore`, тип `lib`) и перенести в него
  `src/agent/` и `src/config.rs` без правки логики; в `crates/core/src/lib.rs`
  объявить `pub mod agent; pub mod config;`. Проверка: `cargo build -p agentcore`
  проходит.
- [x] 1.3 Создать `crates/cli` (`agentcli`, тип `bin`) и перенести в него
  `main.rs`, `cli.rs`, `tui.rs`, `chats.rs`, `clipboard.rs`, `markdown.rs`,
  `assets/`; заменить импорты `crate::agent` / `crate::config` на
  `agentcore::agent` / `agentcore::config`. Проверка: `cargo build` собирает
  workspace без ошибок и предупреждений о неразрешённых импортах.
- [x] 1.4 Развести зависимости по крейтам: в `agentcore` остаются `reqwest`,
  `serde`, `serde_json`, `toml`, `anyhow`, `async-trait`, `dirs`, `tokio`;
  всё, что относится к терминалу (`ratatui`, `crossterm`, `termimad`,
  `indicatif`, `console`, `ansi-to-tui`, `arboard`, `clap`), — только в
  `agentcli`. Проверка: `cargo tree -p agentcore` не содержит `ratatui`
  и `clap`.
- [x] 1.5 Ручная проверка неизменности поведения CLI: `cargo run -p agentcli --
  ask "привет"` отвечает как прежде, `cargo run -p agentcli -- chat` открывает
  TUI, настройки чата и переключение моделей работают.

## 2. Правки ядра, требуемые сервисом

- [x] 2.1 Добавить в `agentcore` модуль `logging` с типом `ExchangeLog`
  (`disabled()` и `to_dir(PathBuf)`), пишущим `requests.jsonl` и
  `responses.jsonl` из фонового потока через `std::sync::mpsc`. Проверка:
  юнит-тест пишет запись во временную директорию и находит её в файле,
  второй тест убеждается, что `disabled()` не создаёт директорию.
- [x] 2.2 Убрать `logs_dir()` на `CARGO_MANIFEST_DIR` из `agent/http.rs`,
  передавать `Arc<ExchangeLog>` в `HttpAgent` при создании. Проверка:
  `grep -r CARGO_MANIFEST_DIR crates/core` ничего не находит.
- [x] 2.3 Ввести `AgentError` (`Timeout`, `Provider { status, message }`,
  `Transport`, `MissingApiKey`, `Decode`) и возвращать его из `HttpAgent`
  и Ollama-агента внутри `anyhow::Error`. Проверка: юнит-тест убеждается,
  что `err.downcast_ref::<AgentError>()` даёт нужный вариант для ответа 500
  провайдера и для таймаута.
- [x] 2.4 Сделать текст ошибки об отсутствующем ключе настраиваемым: ядро
  отдаёт нейтральное сообщение, CLI передаёт свою подсказку про
  `Ctrl+P` и `agentcli config set-key`. Проверка: юнит-тест на нейтральный
  текст в ядре; ручная проверка подсказки в CLI при пустом ключе.
- [x] 2.5 В `agentcli` определить путь логов (`dirs::data_dir()/agentcli/logs`
  с переопределением `AGENTCLI_LOG_DIR`) и передавать его в `HttpAgent`.
  Проверка: запуск `ask` создаёт файлы журнала по новому пути.
- [x] 2.6 Обновить раздел про архитектуру и логи в `README.md` репозитория
  `agent-cli` под структуру workspace и новый путь логов. Проверка: в README
  нет упоминаний «один бинарный крейт» и `logs/` в корне репозитория.

## 3. Конвейер обработки в ядре

- [x] 3.1 Добавить модуль `agentcore::pipeline` с `RequestContext`,
  `PolicyOutcome`, `PolicyPayload`, `PolicyRecord`, `JudgeVerdict`,
  `PipelineOutcome` и трейтами `InputPolicy`, `OutputPolicy`, `Judge`.
  Проверка: `cargo build -p agentcore` проходит, типы экспортированы из
  `lib.rs`.
- [x] 3.2 Реализовать `Pipeline` (агент через `Arc<dyn Agent>`, списки политик,
  необязательный судья) с порядком «нормализация → input → модель → output →
  judge». Проверка: юнит-тест с фиктивным агентом без сети проходит весь
  конвейер и возвращает `PipelineOutcome::Completed`.
- [x] 3.3 Добавить no-op реализации `AllowAllInput`, `AllowAllOutput`,
  `NoopJudge`. Проверка: тест «конвейер без стадий» и тест «конвейер с
  no-op стадиями» дают одинаковый ответ.
- [x] 3.4 Юнит-тесты исходов политик: пропуск, изменение истории (модель
  получает изменённую историю), изменение ответа, отказ на входе (модель
  не вызывается), отказ на выходе, ошибка стадии отличается от отказа.
  Проверка: `cargo test -p agentcore` — все шесть тестов зелёные.
- [x] 3.5 Юнит-тест судьи: вердикт попадает в `PolicyLog`, судья вызывает
  того же агента, отсутствие судьи оставляет вердикт пустым. Проверка:
  фиктивный агент считает вызовы и видит два обращения при включённом судье.
- [x] 3.6 Запушить `agent-cli/main` с ядром, чтобы сервис мог подключить его
  git-зависимостью. Проверка: `cargo add --git https://github.com/Egor-Liadsky/agent-cli agentcore`
  во временном проекте разрешает зависимость.

## 4. Скелет сервиса (репозиторий agent-sever)

- [x] 4.1 Создать репозиторий `/Users/egor_lyadskiy/ai/agent-sever`
  (`git init`, `.gitignore` с `target/` и `.cargo/config.toml`) и крейт
  `agentd` с зависимостями `axum`, `tokio`, `tower`, `tower-http`, `tracing`,
  `tracing-subscriber`, `serde`, `serde_json`, `anyhow`, `uuid`, `async-trait`
  и dev-зависимостью `wiremock`. Проверка: `cargo build` в новом репозитории
  проходит.
- [x] 4.2 Подключить `agentcore` git-зависимостью и добавить локальный
  `.cargo/config.toml` с `[patch]` на `../agent-cli/crates/core`. Проверка:
  `cargo tree | grep agentcore` показывает путь при наличии patch и git-источник
  после его удаления.
- [x] 4.3 Собрать `Router` с `GET /healthz` и запуском через
  `axum::serve`. Проверка: интеграционный тест через `tower::ServiceExt::oneshot`
  получает 200 без запуска сокета.
- [x] 4.4 Добавить `AppState` с единственным `Arc<HttpAgent>`, создаваемым при
  старте. Проверка: тест на два последовательных запроса видит один и тот же
  экземпляр агента (счётчик созданий равен единице).

## 5. Контракт /v1/chat поверх конвейера

- [x] 5.1 Описать типы запроса и ответа (`ChatRequest`, `ChatSettingsDto`,
  `ChatResponse`, `UsageDto`, `TimingDto`, `PolicyDto`) с сериализацией
  по контракту из спецификации `http-api`. Проверка: тесты сериализации на
  запрос с `prompt`, на запрос с `messages`, на форму успешного ответа.
- [x] 5.2 Реализовать валидацию тела: ровно одно из `prompt`/`messages`,
  отказ при наличии `api_key`, отказ при модели вне белого списка, отказ при
  провайдере `ollama` без настроенного адреса. Проверка: четыре теста
  на код 400 и код ошибки `invalid_request`.
- [x] 5.3 Наложить `settings` из запроса на серверные значения по умолчанию
  и собрать `ChatSettings`. Проверка: тест «частичные настройки» — заданные
  поля берутся из запроса, остальные из конфигурации сервиса.
- [x] 5.4 Реализовать обработчик `POST /v1/chat` как вызов `Pipeline`
  (никакой логики стадий в обработчике). Проверка: интеграционный тест
  с провайдером на `wiremock` возвращает 200, заполненные `content`, `model`,
  `usage`, `timing` и пустой `policy`.
- [x] 5.5 Реализовать `GET /v1/models` и `GET /readyz`. Проверка: тест
  на список моделей, равный белому списку конфигурации, и тест на 200/503
  для `/readyz` при валидной и невалидной конфигурации.
- [x] 5.6 Единый конверт ошибок и отображение исходов в коды: 400, 401, 413,
  422 (отказ политики), 429, 502 (`AgentError::Provider`), 504
  (`AgentError::Timeout`); текст ошибки провайдера нормализуется, полный
  остаётся в журнале. Проверка: тесты на 502 и 504 с `wiremock` и тест,
  что тело ответа 502 не содержит адреса апстрима.

## 6. Конфигурация, лимиты и журнал сервиса

- [x] 6.1 Реализовать `AgentdConfig::from_env()` для всех переменных `AGENTD_*`
  и `PORT`, с падением на старте при отсутствии `AGENTD_UPSTREAM_API_KEY`.
  Проверка: юнит-тесты на значения по умолчанию, на `PORT`, на пустой
  `AGENTD_ALLOWED_MODELS` (разрешена только `AGENTD_MODEL`) и на отсутствие
  ключа.
- [x] 6.2 Добавить слой аутентификации по `Authorization: Bearer` со сравнением
  за постоянное время, только для `/v1`. Проверка: тесты «401 без токена»,
  «401 с чужим токеном», «200 для `/healthz` без токена», «200 при пустом
  списке токенов».
- [x] 6.3 Подключить `RequestBodyLimitLayer`, `ConcurrencyLimitLayer` вместе
  с `LoadShedLayer` и `TimeoutLayer` в порядке из design. Проверка: тест на
  413 при превышении тела и тест на 429 при превышении конкурентности.
- [x] 6.4 Настроить `tracing-subscriber` с выбором формата JSON или текст и
  писать на каждый запрос `request_id`, клиента, модель, длительность, статус
  и токены; тексты промптов и ответов — только при включённом отдельном
  признаке. Проверка: тест захватывает вывод подписчика и убеждается, что
  без признака в записи нет текста промпта, а поля телеметрии есть.
- [x] 6.5 Генерировать `request_id` на входе, возвращать в заголовке
  `x-request-id` и в теле, прокидывать в `RequestContext`. Проверка: тест
  сравнивает заголовок и поле тела на успешном ответе и на ошибке.
- [x] 6.6 Реализовать graceful shutdown по `SIGTERM` и `SIGINT`. Проверка:
  тест или ручной прогон, в котором сигнал во время обработки запроса не
  обрывает ответ, а новые соединения не принимаются.

## 7. Поставка

- [ ] 7.1 Написать многостадийный `Dockerfile` (сборка на образе Rust, рантайм
  на `debian:*-slim`), `.dockerignore`, чтение порта из `PORT` и `HEALTHCHECK`
  на `/healthz`. Проверка: `docker build` проходит, `docker run` с ключом
  отвечает 200 на `/healthz`, `docker inspect` показывает состояние `healthy`.
- [x] 7.2 Написать `README.md` сервиса: таблица переменных окружения, пример
  `curl` для `/v1/chat`, предупреждение о выключенной аутентификации при пустом
  списке токенов, порядок обновления зависимости `agentcore`. Проверка: команда
  `curl` из README выполняется против локально запущенного сервиса и
  возвращает 200.
- [ ] 7.3 Финальная проверка обоих репозиториев: `cargo test` в `agent-cli`
  и в `agent-sever` зелёные, сборка образа без локального `[patch]` проходит,
  `agentcli ask` и `agentcli chat` работают как до изменения.
